import { PopupConfigurationStorageItem } from './protocol';

export const defaultConfiguration: PopupConfigurationStorageItem = {
  scrollSize: 50,
  sensitivity: 40,
  selectedSides: ['top', 'bottom'],
};
